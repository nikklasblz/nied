{
  "name": "@nied/schema",
  "version": "0.1.0",
  "description": "Course schema v1 for the nied framework: types, validators, CLI",
  "type": "module",
  "exports": {
    ".": {
      "require": "./src/index.ts",
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "validate": "bun run src/cli.ts",
    "gen:jsonschema": "bun run src/cli.ts --emit-jsonschema"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.6.0",
    "zod": "^4.0.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
